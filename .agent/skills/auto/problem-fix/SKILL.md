---
name: problem-fix
description: "Problem Fix for amazon-affiliate. 28 error→fix pairs."
domain: problem-fix
composesFrom:
  - fix
triggers:
  - glob: "**/*"
autoGenerated: true
enabled: true
---

# Problem Fix

Auto-compiled from **1 real patterns** in **amazon-affiliate**. This skill is auto-routed to agents when working on problem-fix files.

## 🔧 Problem Playbooks

### Known Error → Fix

| Error Pattern | Fix | Seen |
|-------|-----|------|
| `-   const [error, setError] = useState("");` | Fixed null crash in Link — formalizes the data contract with | 9x |
| `deferredStatusError: DynamicLinkResolutionError - ` | Fixed null crash in DynamicLinkResolutionError — prevents nu | 5x |
| `-   status: "created" - "exists" - "error";` | Fixed null crash in ImportResult — parallelizes async operat | 4x |
| `-     throw new HTTPException(429, { message: 'Too` | Fixed null crash in Hono — prevents brute-force and DoS atta | 4x |
| `copyError: string;` | Fixed null crash in Route — formalizes the data contract wit | 4x |
| `-     throw new HTTPException(400, { message: 'Mis` | Fixed null crash in SELECT — parallelizes async operations f | 3x |
| `-       if (!res.ok) { const d = await res.json();` | Fixed null crash in Mapping | 3x |
| `-     } catch (err) { console.error("Failed to fet` | Fixed null crash in Array — prevents null/undefined runtime  | 3x |
| `+     throw new HTTPException(400, { message: 'Inv` | Fixed null crash in HTTPException — improves module reusabil | 3x |
| `+ import { extractApiErrorMessage } from "../../ut` | Fixed null crash in PortalProduct — formalizes the data cont | 3x |
| `-       throw new Error("Failed to load tracking I` | Fixed null crash in Authorization | 3x |
| `+   AmazonProductFetchError,` | problem-fix in dynamic-links.ts | 3x |
| `-   } catch (error) {` | Fixed null crash in Product — parallelizes async operations  | 2x |
| `-     c.executionCtx.waitUntil(recordFailedLogin(c` | Fixed null crash in SELECT — prevents null/undefined runtime | 2x |
| `+     throw new HTTPException(403, { message: 'Onl` | Fixed null crash in HTTPException — prevents null/undefined  | 2x |
| `const [error, setError] = useState("");` | problem-fix in login.tsx | 2x |
| `-     throw new HTTPException(404, { message: 'Pro` | Fixed null crash in AgentProductResolution — offloads heavy  | 2x |
| `-     throw new HTTPException(403, { message: 'Age` | Fixed null crash in Number — prevents null/undefined runtime | 2x |
| `📌 IDE AST Context: Modified symbols likely includ` | Fixed null crash in CapacitorApp — prevents null/undefined r | 2x |
| `- import { extractApiErrorMessage } from "../../ut` | Fixed null crash in Route — avoids unnecessary re-renders in | 2x |

## 📐 Conventions & Best Practices

### Compiled Rules
- ✅ When encountering this, fix by: problem-fix in dynamic-links.ts (seen 2x)

---
*Auto-generated by BrainSync 🧠 | 1 patterns | 2026-04-03*
