> **BrainSync Context Pumper** 🧠
> Dynamically loaded for active file: `server/services/google-sheets.ts` (Domain: **Generic Logic**)

### 📐 Generic Logic Conventions & Fixes
- **[what-changed] what-changed in google-sheets.ts**: File updated (external): server/services/google-sheets.ts

Content summary (267 lines):
const SHEETS_SCOPE = "https://www.googleapis.com/auth/spreadsheets";
const TOKEN_AUDIENCE = "https://oauth2.googleapis.com/token";

interface SheetProperties {
  sheetId: number;
  title: string;
}

interface SpreadsheetMetadata {
  sheets?: Array<{ properties?: SheetProperties }>;
}

interface GoogleServiceAccountCredentials {
  clientEmail: string;
  privateKey: string;
}

export async function readSheetRows(input: {
  credentials: GoogleServiceAccountCredentials;
  spreadsheetId: string;
  gi
- **[problem-fix] Fixed null crash in SheetSyncConfig — prevents null/undefined runtime crashes**: -   ensureProductRecord,
+   parseSpreadsheetReference,
-   parseSheetProductRow,
+   readSheetRows,
- } from "./product-ingestion";
+   writeSheetRows,
- 
+ } from "./google-sheets";
- interface SheetSyncConfig {
+ import { ensureProductRecord, isValidAsin } from "./product-ingestion";
-   id: number;
+ 
-   sheet_url: string | null;
+ interface SheetSyncConfig {
-   default_marketplace: string;
+   id: number;
-   mirror_webhook_url: string | null;
+   sheet_url: string | null;
-   is_active: number;
+   sheet_tab_name: string | null;
-   last_imported_at: string | null;
+   default_marketplace: string;
-   last_exported_at: string | null;
+   is_active: number;
-   created_at: string;
+   last_imported_at: string | null;
-   updated_at: string;
+   last_exported_at: string | null;
- }
+   created_at: string;
- 
+   updated_at: string;
- interface SheetSyncLog {
+ }
-   id: number;
+ 
-   direction: "import" | "export";
+ interface SheetSyncLog {
-   status: "success" | "failed";
+   id: number;
-   total_rows: number;
+   direction: "import" | "export";
-   created_count: number;
+   status: "success" | "failed";
-   updated_count: number;
+   total_rows: number;
-   skipped_count: number;
+   created_count: number;
-   details: string;
+   updated_count: number;
-   error_message: string | null;
+   skipped_count: number;
-   triggered_by_username: string | null;
+   details: string;
-   created_at: string;
+   error_message: string | null;
-   finished_at: string | null;
+   triggered_by_username: string | null;
- }
+   created_at: string;
- 
+   finished_at: string | null;
- interface SyncProductsFromSheetInput {
+ }
-   db: D1Database;
+ 
-   kv: KVNamespace;
+ interface GoogleCredentials {
-   apiKey?: string;
+   clientEmail: string;
-   config: SheetSyncConfig;
+   privateKey: string;
-   triggeredByUserId?: number;
+ }
- }
+ 
- 
+ interface SyncProductsFromSheetInput {
- interface MirrorProductsToSheetInput {
+   db: D1Database;
-   db: D1Database;
+   kv
… [diff truncated]

📌 IDE AST Context: Modified symbols likely include [SheetSyncConfig, SheetSyncLog, GoogleCredentials, SyncProductsFromSheetInput, MirrorProductsToSheetInput]
- **[what-changed] what-changed in sheet-sync.ts**: File updated (external): server/services/sheet-sync.ts

Content summary (491 lines):
import { CacheService } from "./cache";
import {
  ensureProductRecord,
  parseSheetProductRow,
} from "./product-ingestion";

interface SheetSyncConfig {
  id: number;
  sheet_url: string | null;
  default_marketplace: string;
  mirror_webhook_url: string | null;
  is_active: number;
  last_imported_at: string | null;
  last_exported_at: string | null;
  created_at: string;
  updated_at: string;
}

interface SheetSyncLog {
  id: number;
  direction: "import" | "export";
  status: "success" | "f
- **[what-changed] what-changed in product-ingestion.ts**: File updated (external): server/services/product-ingestion.ts

Content summary (255 lines):
interface AmazonProductData {
  title: string;
  imageUrl: string;
  category: string | null;
  description: string | null;
  features: string[];
}

interface ProductRecord {
  id: number;
  asin: string;
  title: string;
  image_url: string;
  marketplace: string;
  category: string | null;
  status?: string;
}

interface EnsureProductInput {
  db: D1Database;
  asin: string;
  marketplace: string;
  apiKey?: string;
  title?: string | null;
  imageUrl?: string | null;
  category?: string | nul
- **[what-changed] what-changed in product-submissions.ts**: File updated (external): .react-router/types/app/routes/admin/+types/product-submissions.ts

Content summary (65 lines):
// Generated by React Router

import type { GetInfo, GetAnnotations } from "react-router/internal";

type Module = typeof import("../product-submissions.js")

type Info = GetInfo<{
  file: "routes/admin/product-submissions.tsx",
  module: Module
}>

type Matches = [{
  id: "root";
  module: typeof import("../../../root.js");
}, {
  id: "routes/admin/layout";
  module: typeof import("../layout.js");
}, {
  id: "routes/admin/product-submissions";
  module: typeof import("../product-submissions.js"
- **[what-changed] what-changed in reports.ts**: File updated (external): .react-router/types/app/routes/admin/+types/reports.ts

Content summary (65 lines):
// Generated by React Router

import type { GetInfo, GetAnnotations } from "react-router/internal";

type Module = typeof import("../reports.js")

type Info = GetInfo<{
  file: "routes/admin/reports.tsx",
  module: Module
}>

type Matches = [{
  id: "root";
  module: typeof import("../../../root.js");
}, {
  id: "routes/admin/layout";
  module: typeof import("../layout.js");
}, {
  id: "routes/admin/reports";
  module: typeof import("../reports.js");
}];

type Annotations = GetAnnotations<Info &
- **[convention] what-changed in analytics.ts — confirmed 3x**: File updated (external): .react-router/types/app/routes/portal/+types/analytics.ts

Content summary (65 lines):
// Generated by React Router

import type { GetInfo, GetAnnotations } from "react-router/internal";

type Module = typeof import("../analytics.js")

type Info = GetInfo<{
  file: "routes/portal/analytics.tsx",
  module: Module
}>

type Matches = [{
  id: "root";
  module: typeof import("../../../root.js");
}, {
  id: "routes/portal/layout";
  module: typeof import("../layout.js");
}, {
  id: "routes/portal/analytics";
  module: typeof import("../analytics.js");
}];

type Annotations = GetAnnotat
- **[what-changed] what-changed in asin-new.ts**: File updated (external): .react-router/types/app/routes/portal/+types/asin-new.ts

Content summary (65 lines):
// Generated by React Router

import type { GetInfo, GetAnnotations } from "react-router/internal";

type Module = typeof import("../asin-new.js")

type Info = GetInfo<{
  file: "routes/portal/asin-new.tsx",
  module: Module
}>

type Matches = [{
  id: "root";
  module: typeof import("../../../root.js");
}, {
  id: "routes/portal/layout";
  module: typeof import("../layout.js");
}, {
  id: "routes/portal/asin-new";
  module: typeof import("../asin-new.js");
}];

type Annotations = GetAnnotations
- **[what-changed] Updated API endpoint routes**: -     route("asins/new", "routes/portal/products.tsx"),
+     route("asins/new", "routes/portal/asin-new.tsx"),

📌 IDE AST Context: Modified symbols likely include [default]
- **[what-changed] Replaced auth RouteFiles — reduces initial bundle size with code splitting**: -   "/admin/tracking": {
+   "/admin/product-submissions": {
-   "/admin/mappings": {
+   "/admin/tracking": {
-   "/admin/analytics": {
+   "/admin/mappings": {
-   "/admin/audit-logs": {
+   "/admin/analytics": {
-   "/portal/login": {
+   "/admin/reports": {
-   "/portal": {
+   "/admin/audit-logs": {
-   "/portal/products": {
+   "/portal/login": {
-   "/portal/links": {
+   "/portal": {
- };
+   "/portal/asins/new": {
- 
+     params: {};
- type RouteFiles = {
+   };
-   "root.tsx": {
+   "/portal/products": {
-     id: "root";
+     params: {};
-     page: "/" | "/:agent/:asin" | "/privacy" | "/disclosure" | "/about" | "/contact" | "/deals" | "/deals/:asin" | "/category/:slug" | "/admin/login" | "/admin" | "/admin/users" | "/admin/agents" | "/admin/products" | "/admin/tracking" | "/admin/mappings" | "/admin/analytics" | "/admin/audit-logs" | "/portal/login" | "/portal" | "/portal/products" | "/portal/links";
+   };
-   };
+   "/portal/links": {
-   "routes/bridge.tsx": {
+     params: {};
-     id: "routes/bridge";
+   };
-     page: "/:agent/:asin";
+   "/portal/analytics": {
-   };
+     params: {};
-   "routes/public-layout.tsx": {
+   };
-     id: "routes/public-layout";
+ };
-     page: "/" | "/privacy" | "/disclosure" | "/about" | "/contact" | "/deals" | "/deals/:asin" | "/category/:slug";
+ 
-   };
+ type RouteFiles = {
-   "routes/home.tsx": {
+   "root.tsx": {
-     id: "routes/home";
+     id: "root";
-     page: "/";
+     page: "/" | "/:agent/:asin" | "/privacy" | "/disclosure" | "/about" | "/contact" | "/deals" | "/deals/:asin" | "/category/:slug" | "/admin/login" | "/admin" | "/admin/users" | "/admin/agents" | "/admin/products" | "/admin/product-submissions" | "/admin/tracking" | "/admin/mappings" | "/admin/analytics" | "/admin/reports" | "/admin/audit-logs" | "/portal/login" | "/portal" | "/portal/asins/new" | "/portal/products" | "/portal/links" | "/portal/analytics";
-   "routes/privacy.tsx": {
+   "routes/bridge.tsx": {
-     id: "routes/priv
… [diff truncated]

📌 IDE AST Context: Modified symbols likely include ["react-router", Pages, RouteFiles, RouteModules]
- **[convention] Fixed null crash in SELECT — parallelizes async operations for speed — confirmed 5x**: -   const updates: string[] = [];
+   const relatedAgents = await c.env.DB.prepare(
-   const values: (string | number | null)[] = [];
+     `SELECT a.slug
- 
+      FROM agent_products ap
-   if (body.title) { updates.push('title = ?'); values.push(body.title); }
+      JOIN agents a ON a.id = ap.agent_id
-   if (body.image_url) { updates.push('image_url = ?'); values.push(body.image_url); }
+      WHERE ap.product_id = ?`
-   if (body.category !== undefined) { updates.push('category = ?'); values.push(body.category); }
+   )
-   if (body.is_active !== undefined) { updates.push('is_active = ?'); values.push(body.is_active ? 1 : 0); }
+     .bind(id)
-   if (body.status !== undefined) { updates.push('status = ?'); values.push(body.status); }
+     .all<{ slug: string }>();
-   if (updates.length === 0) {
+   const updates: string[] = [];
-     throw new HTTPException(400, { message: 'No fields to update' });
+   const values: (string | number | null)[] = [];
-   }
+ 
- 
+   if (body.title) { updates.push('title = ?'); values.push(body.title); }
-   updates.push('updated_at = CURRENT_TIMESTAMP');
+   if (body.image_url) { updates.push('image_url = ?'); values.push(body.image_url); }
-   values.push(id);
+   if (body.category !== undefined) { updates.push('category = ?'); values.push(body.category); }
- 
+   if (body.is_active !== undefined) { updates.push('is_active = ?'); values.push(body.is_active ? 1 : 0); }
-   await c.env.DB.prepare(`UPDATE products SET ${updates.join(', ')} WHERE id = ?`)
+   if (body.status !== undefined) { updates.push('status = ?'); values.push(body.status); }
-     .bind(...values)
+ 
-     .run();
+   if (updates.length === 0) {
- 
+     throw new HTTPException(400, { message: 'No fields to update' });
-   const cache = new CacheService(c.env.KV);
+   }
-   c.executionCtx.waitUntil(cache.invalidateForProduct(product.asin));
+ 
- 
+   updates.push('updated_at = CURRENT_TIMESTAMP');
-   const updated = await c.env.DB.prepare('SELECT * FROM p
… [diff truncated]

📌 IDE AST Context: Modified symbols likely include [products, products.get('/') callback, products.get('/submissions') callback, products.post('/') callback, products.post('/fetch-asin') callback]
- **[what-changed] Replaced auth RouteConfig**: -     route("tracking", "routes/admin/tracking.tsx"),
+     route("product-submissions", "routes/admin/product-submissions.tsx"),
-     route("mappings", "routes/admin/mappings.tsx"),
+     route("tracking", "routes/admin/tracking.tsx"),
-     route("analytics", "routes/admin/analytics.tsx"),
+     route("mappings", "routes/admin/mappings.tsx"),
-     route("audit-logs", "routes/admin/audit-logs.tsx"),
+     route("analytics", "routes/admin/analytics.tsx"),
-   ]),
+     route("reports", "routes/admin/reports.tsx"),
- 
+     route("audit-logs", "routes/admin/audit-logs.tsx"),
-   route("portal/login", "routes/portal/login.tsx"),
+   ]),
-   route("portal", "routes/portal/layout.tsx", [
+ 
-     index("routes/portal/dashboard.tsx"),
+   route("portal/login", "routes/portal/login.tsx"),
-     route("products", "routes/portal/products.tsx"),
+   route("portal", "routes/portal/layout.tsx", [
-     route("links", "routes/portal/links.tsx"),
+     index("routes/portal/dashboard.tsx"),
-   ]),
+     route("asins/new", "routes/portal/products.tsx"),
- ] satisfies RouteConfig;
+     route("products", "routes/portal/products.tsx"),
- 
+     route("links", "routes/portal/links.tsx"),
+     route("analytics", "routes/portal/analytics.tsx"),
+   ]),
+ ] satisfies RouteConfig;
+ 

📌 IDE AST Context: Modified symbols likely include [default]
- **[what-changed] what-changed in public.ts**: -     WHERE is_active = 1
+     WHERE is_active = 1 AND status = 'active'
-     WHERE p.is_active = 1 AND c.slug = ?
+     WHERE p.is_active = 1 AND p.status = 'active' AND c.slug = ?
-     WHERE is_active = 1
+     WHERE is_active = 1 AND status = 'active'
-     "SELECT COUNT(*) as total FROM products WHERE is_active = 1"
+     "SELECT COUNT(*) as total FROM products WHERE is_active = 1 AND status = 'active'"

📌 IDE AST Context: Modified symbols likely include [router, router.get("/categories") callback, router.get("/products/featured") callback, router.get("/products/category/:slug") callback, router.get("/products") callback]
- **[what-changed] what-changed in page.ts**: -        AND ap.is_active = 1 AND a.is_active = 1 AND p.is_active = 1
+        AND ap.is_active = 1 AND a.is_active = 1 AND p.is_active = 1 AND p.status = 'active'

📌 IDE AST Context: Modified symbols likely include [page, page.get('/:agentSlug/:asin') callback, recordViewAsync, default]
- **[what-changed] what-changed in redirect.ts**: -          AND ap.is_active = 1 AND a.is_active = 1 AND p.is_active = 1
+          AND ap.is_active = 1 AND a.is_active = 1 AND p.is_active = 1 AND p.status = 'active'

📌 IDE AST Context: Modified symbols likely include [redirect, RedirectContext, redirect.get('/:agentSlug/:asin') callback, default]
